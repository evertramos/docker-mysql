name: ${EZY_MYSQL_PROJECT_NAME:-docker-mysql}

services:
  mysql:
    image: mysql/mysql-server:${EZY_MYSQL_VERSION:-latest}
    container_name: ${EZY_MYSQL_CONTAINER_NAME:-mysql}
    restart: unless-stopped
    ports:
      - ${EZY_MYSQL_PORT-3306}:3306
    volumes:
      - ./data:/var/lib/mysql

